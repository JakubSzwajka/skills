{
  "name": "claude-blocks-connector",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Hosts inbound Blocks requests and pipes in a live Claude Code session via MCP; outbound work belongs to @blocks-network/mcp-server.",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --experimental-strip-types --test \"test/**/*.test.ts\"",
    "start": "node --experimental-strip-types index.ts"
  },
  "dependencies": {
    "@blocks-network/sdk": "^1.0.15",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "ws": "^8.18.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.13"
  }
}
