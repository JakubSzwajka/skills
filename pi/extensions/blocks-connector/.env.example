# Copy to .env in the trusted directory where you start Pi. Never commit real values or paste them into chat.
BLOCKS_API_KEY=

# Optional overrides
# PI_BLOCKS_CARD=agent-card.json
# PI_BLOCKS_MAX_CONCURRENT=3
# PI_BLOCKS_REPLY_TIMEOUT_MS=1500000
# BLOCKS_CDM_URL=https://config.blocks.ai/config.json   # enterprise deployments only
